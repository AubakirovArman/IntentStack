section:
  id: docs_content
  type: content
  title: IntentStack Documentation
  blocks:
    - id: overview
      type: heading
      level: 2
      text: What IntentStack is
    - id: overview_text
      type: paragraph
      text: >-
        IntentStack is an AI-native fullstack compiler. The application is described as modular
        intent: a small root manifest includes focused files under shared, frontend, and backend
        folders. The compiler assembles those modules and generates framework code, routes, API
        handlers, database files, styles, and migrations.
    - id: generated
      type: heading
      level: 2
      text: What it can generate
    - id: generated_list
      type: list
      items:
        - Multi-page React/Vite applications for the web_ts_minimal target.
        - Next.js App Router applications for the next_shadcn target.
        - >-
          Shared navigation, landing sections, documentation content, forms, tables, record detail
          pages, stats, pricing cards, and custom component wrappers.
        - CRUD APIs, Drizzle/SQLite schema, migrations, zod validators, and generated API clients.
        - Auth, workflow, and integration scaffolding for the supported MVP contracts.
    - id: workflow
      type: heading
      level: 2
      text: Agent workflow
    - id: workflow_list
      type: list
      items:
        - Read the root intent and the relevant owner module before changing behavior.
        - Express the next change as a semantic patch instead of editing generated code.
        - Run intentstack apply as a dry run and inspect the semantic diff.
        - Run intentstack apply --write so the compiler writes back to the owner module.
        - Run intentstack check, intentstack build, then verify the generated application.
    - id: command
      type: code
      language: powershell
      code: >
        node ..\..\intentstack\src\index.js apply .\patches\004-write-tool-documentation.yaml
        --project .

        node ..\..\intentstack\src\index.js check --project .

        node ..\..\intentstack\src\index.js build --project .
    - id: core_loop
      type: heading
      level: 2
      text: Core compiler loop
    - id: core_loop_list
      type: list
      items:
        - Parse the intent file.
        - Validate schema, target capabilities, references, auth policies, and component contracts.
        - Build a target-neutral Core IR.
        - Plan generated files before writing them.
        - Emit target-specific frontend, backend, database, and support files.
    - id: modular_first
      type: heading
      level: 2
      text: Modular structure is the foundation
    - id: modular_first_text
      type: paragraph
      text: >-
        A real app should not grow as one huge YAML file. Keep the root file as a manifest and put
        behavior into small owner modules. This gives agents a precise edit target and keeps
        diagnostics actionable.
    - id: modular_tree
      type: code
      language: text
      code: |
        intent/
          app.intent.yaml                 # version, project, includes
          shared/
            navigation.yaml               # global navigation
            theme.yaml                    # design tokens
          frontend/
            pages/
              home.page.yaml              # route + section refs
              docs.page.yaml
            sections/
              home/hero.section.yaml      # focused UI modules
              docs/docs-content.section.yaml
          backend/
            entities/lead.entity.yaml     # data model modules
            actions/create-lead.action.yaml
    - id: modular_patch_writeback
      type: callout
      title: Patch writeback
      text: >-
        In a modular project, intentstack apply --write preserves the module layout. A docs copy
        change writes to the docs section file, a navigation change writes to
        shared/navigation.yaml, and an entity change writes to backend/entities.
    - id: semantic_patches
      type: heading
      level: 2
      text: Semantic patches
    - id: semantic_patches_text
      type: paragraph
      text: >-
        Patches are small machine-readable changes such as navigation.item.add, page.create,
        section.add, entity.field.add, and content.block.update. The compiler validates the
        assembled result before writing changes back to the correct owner module.
    - id: targets
      type: heading
      level: 2
      text: Targets
    - id: targets_list
      type: list
      items:
        - web_ts_minimal emits Vite, React, Tailwind/daisyUI, Hono API, Drizzle, and SQLite.
        - >-
          next_shadcn emits Next.js App Router, shadcn-style primitives, route handlers, Drizzle,
          and SQLite.
        - >-
          The same intent can compile to both targets when each declared component and action is
          supported.
    - id: diagnostics
      type: heading
      level: 2
      text: Diagnostics and safety
    - id: diagnostics_list
      type: list
      items:
        - >-
          Errors and warnings include code, severity, message, path, suggestion, and sometimes
          fix_hint.
        - >-
          Unsupported fields, actions, components, routes, and references are rejected before
          generation.
        - Generated zones are disposable; custom code belongs outside generated folders.
        - diff, graph, stats, security, and verify commands help audit intent and output.
    - id: authoring_examples
      type: heading
      level: 2
      text: Authoring examples
    - id: authoring_examples_intro
      type: paragraph
      text: >-
        Each example below is paired: the generated live component appears first, and the patch code
        that creates the same kind of component appears directly below it.
    - id: authoring_matrix
      type: table
      columns:
        - Goal
        - Patch ops
        - Generated result
      rows:
        - - Add cards
          - section.add
          - React card grid component
        - - Add a form
          - entity.create + action.create + form.add
          - Form UI plus POST API route
        - - Add a table
          - action.create + table.add
          - Data table plus GET API client
        - - Add plans or metrics
          - section.add
          - Pricing or stats section
        - - Add a page
          - page.create + navigation.item.add
          - New route and nav item
    - id: authoring_note
      type: callout
      title: Practical rule
      text: >-
        Keep each patch focused. One page, one section, or one entity change is easier to review and
        easier to roll back.
    - id: live_component_demo_note
      type: callout
      title: Live example + code
      text: >-
        Read each example as a pattern: inspect the generated component, then copy the patch below
        it and adapt the ids, text, fields, or records for your app.
    - id: cards_example
      type: example
      title: Card grid
      text: >-
        A card_grid section is useful for feature cards, service lists, galleries, and compact
        repeated content. The preview is generated from the section module; the patch below shows
        how to create the same kind of module.
      section: docs_component_cards
      language: yaml
      code: |
        version: 0.1
        patch:
          - op: section.module.add
            page: docs
            after: docs_content
            section:
              id: docs_component_cards
              type: card_grid
              title: Live card_grid example
              embed_only: true
              columns: 3
              items:
                - title: Card title
                  text: A card_grid section renders repeated cards from structured items.
                - title: Agent-friendly
                  text: Each card is data, so an agent can add, remove, or update cards safely.
                - title: Target-owned UI
                  text: The intent declares the content; the target adapter owns the React markup.
    - id: form_example
      type: example
      title: Form
      text: >-
        A form usually needs an entity and a create_record action. The compiler then generates the
        UI, request handler, validation schema, database insert, and API client.
      section: docs_component_form
      language: yaml
      code: |
        version: 0.1
        patch:
          - op: entity.create
            id: DemoRequest
            table: demo_requests
            fields:
              - id: name
                type: string
                required: true
              - id: email
                type: string
                required: true
              - id: project_type
                type: enum
                values: [interior, product, animation]
                required: true
              - id: budget
                type: number
              - id: message
                type: text
          - op: action.create
            id: create_demo_request
            type: create_record
            entity: DemoRequest
          - op: section.module.add
            page: docs
            after: docs_component_cards
            section:
              id: docs_component_form
              type: form
              title: Live form example
              embed_only: true
              entity: DemoRequest
              fields: [name, email, project_type, budget, message]
              submit:
                action: create_demo_request
                success_message: Demo request saved.
    - id: table_example
      type: example
      title: Table
      text: >-
        A table connects to a list_records action. The generated app owns loading, empty, and error
        states while the intent declares the entity, source action, and visible columns.
      section: docs_component_table
      language: yaml
      code: |
        version: 0.1
        patch:
          - op: action.create
            id: list_demo_requests
            type: list_records
            entity: DemoRequest
          - op: section.module.add
            page: docs
            after: docs_component_form
            section:
              id: docs_component_table
              type: table
              embed_only: true
              entity: DemoRequest
              source:
                action: list_demo_requests
              columns: [name, email, project_type, budget]
    - id: pricing_example
      type: example
      title: Pricing cards
      text: >-
        pricing_cards is a structured marketing section. Plans, prices, and features stay editable
        as data while each target controls presentation.
      section: docs_component_pricing
      language: yaml
      code: |
        version: 0.1
        patch:
          - op: section.module.add
            page: docs
            after: docs_component_table
            section:
              id: docs_component_pricing
              type: pricing_cards
              title: Live pricing_cards example
              embed_only: true
              items:
                - title: Starter
                  price: from $300
                  features: [One room or product, Two camera angles, Basic material pass]
                - title: Studio
                  price: from $900
                  features: [Three scenes, Styled lighting, Revision pass]
                - title: Launch
                  price: custom
                  features: [Landing page visuals, Batch export, Motion-ready frames]
    - id: stats_example
      type: example
      title: Stats
      text: >-
        stats works well for product metrics, delivery promises, usage numbers, and operational
        summaries. Keep each stat as data so future patches can update one value safely.
      section: docs_component_stats
      language: yaml
      code: |
        version: 0.1
        patch:
          - op: section.module.add
            page: docs
            after: docs_component_pricing
            section:
              id: docs_component_stats
              type: stats
              title: Live stats example
              embed_only: true
              items:
                - label: First draft
                  value: 3 days
                  text: Typical initial delivery window.
                - label: Formats
                  value: 6+
                  text: Common web and presentation exports.
                - label: Modules
                  value: 100%
                  text: This page is assembled from section modules.
    - id: current_limits
      type: heading
      level: 2
      text: Current limits
    - id: current_limits_list
      type: list
      items:
        - There is not yet a visual browser editor; editing is done through intent and patch files.
        - >-
          Content is intentionally structured, so it is reliable for agents but not yet a full rich
          text editor.
        - Generated dependency versions still need an audit-hardening pass.
        - >-
          Production auth, durable workflows, deployments, and provider-specific integrations need
          deeper hardening.
