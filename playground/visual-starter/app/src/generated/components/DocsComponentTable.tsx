/**
 * @generated by IntentStack (target: web_ts_minimal). DO NOT EDIT BY HAND.
 * Source of truth: intent/app.intent.yaml - re-run `intentstack build` to regenerate.
 */
import { useEffect, useState } from "react";
import { listDemoRequest } from "../api/client";

export function DocsComponentTable() {
  const [rows, setRows] = useState<Array<Record<string, unknown>>>([]);
  const [loading, setLoading] = useState(true);

  async function loadRows() {
    setLoading(true);
    const nextRows = await listDemoRequest();
    setRows(nextRows);
    setLoading(false);
  }

  useEffect(() => {
    void loadRows();
  }, []);
  return (
    <section className="p-6">
      <div className="overflow-x-auto bg-base-100 rounded-lg border border-base-200">
        <table className="table">
          <thead>
            <tr>
              <th>{"Name"}</th>
              <th>{"Email"}</th>
              <th>{"Project type"}</th>
              <th>{"Budget"}</th>
            </tr>
          </thead>
          <tbody>
            {loading && (
              <tr>
                <td colSpan={4} className="text-center py-8">
                  <span className="loading loading-spinner" />
                </td>
              </tr>
            )}
            {!loading && rows.length === 0 && (
              <tr>
                <td colSpan={4} className="text-center opacity-60 py-8">
                  No records yet
                </td>
              </tr>
            )}
            {!loading &&
              rows.map((row, i) => (
                <tr key={i}>
                  <td>{String(row["name"] ?? "")}</td>
                  <td>{String(row["email"] ?? "")}</td>
                  <td>{String(row["project_type"] ?? "")}</td>
                  <td>{String(row["budget"] ?? "")}</td>
                </tr>
              ))}
          </tbody>
        </table>
      </div>
    </section>
  );
}
