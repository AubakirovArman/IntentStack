/* @generated by IntentStack (target: next_shadcn). DO NOT EDIT — edit intent/app.intent.yaml and rebuild. */
"use client";
import { useEffect, useState } from "react";
import {
  Table,
  TableHeader,
  TableBody,
  TableRow,
  TableHead,
  TableCell,
} from "@/components/ui/table";
import { listDemoRequest } from "@/lib/api/client";

export function DocsComponentTable() {
  const [rows, setRows] = useState<Array<Record<string, unknown>>>([]);
  const [loading, setLoading] = useState(true);

  async function loadRows() {
    setLoading(true);
    const nextRows = await listDemoRequest();
    setRows(nextRows);
    setLoading(false);
  }

  useEffect(() => {
    void loadRows();
  }, []);
  return (
    <div className="mx-auto max-w-6xl px-4 py-6">
      <div className="rounded-lg border">
        <Table>
          <TableHeader>
            <TableRow>
              <TableHead>{"Name"}</TableHead>
              <TableHead>{"Email"}</TableHead>
              <TableHead>{"Project type"}</TableHead>
              <TableHead>{"Budget"}</TableHead>
            </TableRow>
          </TableHeader>
          <TableBody>
            {loading && (
              <TableRow>
                <TableCell
                  colSpan={4}
                  className="text-center text-muted-foreground"
                >
                  Loading…
                </TableCell>
              </TableRow>
            )}
            {!loading && rows.length === 0 && (
              <TableRow>
                <TableCell
                  colSpan={4}
                  className="text-center text-muted-foreground"
                >
                  No records yet
                </TableCell>
              </TableRow>
            )}
            {!loading &&
              rows.map((row, i) => (
                <TableRow key={i}>
                  <TableCell>{String(row["name"] ?? "")}</TableCell>
                  <TableCell>{String(row["email"] ?? "")}</TableCell>
                  <TableCell>{String(row["project_type"] ?? "")}</TableCell>
                  <TableCell>{String(row["budget"] ?? "")}</TableCell>
                </TableRow>
              ))}
          </TableBody>
        </Table>
      </div>
    </div>
  );
}
