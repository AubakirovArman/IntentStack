/**
 * @generated by IntentStack (target: web_ts_minimal). DO NOT EDIT BY HAND.
 * Source of truth: intent/app.intent.yaml - re-run `intentstack build` to regenerate.
 */
const BASE = import.meta.env.VITE_API_URL ?? ''

function csrfToken() {
  if (typeof document === 'undefined') return ''
  const match = document.cookie.split('; ').find((part) => part.startsWith('intentstack_csrf='))
  return match ? decodeURIComponent(match.split('=').slice(1).join('=')) : ''
}

function csrfHeaders(): Record<string, string> {
  const token = csrfToken()
  return token ? { 'X-CSRF-Token': token } : {}
}

function tenantHeaders(): Record<string, string> { return {} }
function tenantQuery() { return '' }


function websocketUrl(path: string) {
  const origin = BASE || (typeof window !== 'undefined' ? window.location.origin : 'http://localhost:8787')
  const url = new URL(path, origin)
  url.protocol = url.protocol === 'https:' ? 'wss:' : 'ws:'
  return url.toString()
}

export async function createLead(payload: Record<string, unknown>) {
  const res = await fetch(`${BASE}/api/leads`, {
    method: 'POST',
    credentials: 'include',
    headers: { 'Content-Type': 'application/json', ...tenantHeaders(), ...csrfHeaders() },
    body: JSON.stringify(payload),
  })
  const json = await res.json().catch(() => ({}))
  return { ok: res.ok, data: json.data, error: json.error }
}

export async function listLead(): Promise<Array<Record<string, unknown>>> {
  const res = await fetch(`${BASE}/api/leads`, { credentials: 'include', headers: tenantHeaders() })
  const json = await res.json().catch(() => ({ data: [] }))
  return (json.data ?? []) as Array<Record<string, unknown>>
}

export async function getLead(id: number): Promise<{ ok: boolean; data?: Record<string, unknown>; error?: unknown }> {
  const res = await fetch(BASE + '/api/leads/' + id, { credentials: 'include', headers: tenantHeaders() })
  const json = await res.json().catch(() => ({}))
  return { ok: res.ok, data: json.data, error: json.error }
}

export async function updateLead(id: number, payload: Record<string, unknown>) {
  const res = await fetch(BASE + '/api/leads/' + id, {
    method: 'PUT',
    credentials: 'include',
    headers: { 'Content-Type': 'application/json', ...tenantHeaders(), ...csrfHeaders() },
    body: JSON.stringify(payload),
  })
  const json = await res.json().catch(() => ({}))
  return { ok: res.ok, data: json.data, error: json.error }
}

